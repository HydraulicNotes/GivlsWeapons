/* using Terraria.ID;

namespace GivlsWeapons.Common.Projectiles
{
    public class ProjectileAIViewer : GlobalProjectile
    {
        public override void AI(Projectile projectile)
        {
            if(projectile.type == ProjectileID.Flamelash)
            {
                Main.NewText(projectile.rotation);
            }
        }
    }
} */

//Debug class for checking information about vanilla projectiles. Uncomment only when necessary